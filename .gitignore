node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Local env; use .env.example as the template
.env
.env.local
.env.*.local
# Wrangler reads this for local `wrangler dev` (Worker secrets/vars). Never commit.
.dev.vars

# Wrangler local state and build cache. Production deploys build fresh from worker/index.ts.
.wrangler/

# Tooling / scratch
.replit
.agents/
.claude/

# attached_assets/ is a mix of scratch files and live site assets.
# Only the WebP files (imported via @assets alias) should be committed.
attached_assets/*
!attached_assets/*.webp